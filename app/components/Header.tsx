import Nav from "./Nav";

const Header = () => {
  return (
    <header className="py-2 mb-2 sticky top-0 bg-[var(--background-start)] z-50">
      <Nav />
    </header>
  );
};

export default Header;
